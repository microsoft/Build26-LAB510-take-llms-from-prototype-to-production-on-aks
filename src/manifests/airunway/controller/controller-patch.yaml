apiVersion: apps/v1
kind: Deployment
metadata:
  name: airunway-controller-manager
  namespace: airunway-system
spec:
  template:
    spec:
      containers:
      - name: manager
        ports:
        - containerPort: 8443
          name: https
          protocol: TCP

